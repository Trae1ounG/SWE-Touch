# Harbor Runtime

This directory contains the Harbor 0.13.2 fork used by SWE-Touch. The fork adds the
SWE-Touch construction pipeline, runtime interventions, paired evaluation commands, and
the Mini-SWE-Agent integration while preserving Harbor's package layout.

Run commands from the repository root with `uv run --project harbor harbor ...`.
